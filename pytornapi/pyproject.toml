[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "PyTornApi"
version = "0.1.0"
description = "A simple torn api wrapper or whatever"
authors = [
    {name = "robert-ish"}
]
license = "MIT"
classifiers = [
    "Programming Language :: Python :: 3"
]
requires-python = ">=3.8"
dependencies = [
    "requests>=2.25.0",
]